import sqlite3

def get_db_connection()
conn = sqlite3.connect('database.db')
conn.row_factory = sqlie3.Row
return conn

def init_db():
    conn = get_db_connection()
    conn.execute('''
    create tabel if not exists items (
    id integer primary key autoincrement,
    name text not null,
    description text
    )
 ''')
 conn.comit